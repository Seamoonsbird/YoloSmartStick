# 安装labelme
~~呃，这个怎么安装`labelimg`就怎么装吧，因人而异吧，每个电脑宝宝都有自己的特殊体质。~~
参考`README.md`中安装`labelimg`步骤
## 进入虚拟环境
在`Anaconda prompt`里面输入
```bash
conda activate xxx
```
## 利用pip安装
```bash
pip install labelme
```

# 打开并使用labelme
像使用`labelimg`一样在命令行里面输入
```bash
labelme
```
## 选择路径
点击打开目录，选择你的数据集目录即可
**这个和`labelimg`有一点不同，这个不用选择存放标签的地方，因为他的标签文件和源文件是放在一起的**

## 打开自动保存
点击左上角`文件`，点击`自动保存`

## 快捷键
与labelimg相差无几

# 工作流程
## 收集数据
> 数据要求
1.一般不要同一个地方拍好几张，一张就够了，不然会把一些不是特征的识别为特征，导致数据集质量下降
2.如果你觉得某一个地方的特征非常独特，就可以拍多几个角度（也要光线变换大一点点，不过最好还是找找有没有更多类似的地方，原因同1.）
3.数据集打标签名字要提前统一好，比如上台阶统一命名为upstairs，不要一个人叫upstairs，一个人叫upstair，不然等于白打，这个到时候会在项目根目录有个txt文档写的，到时候有新的标签名字在群里说一声再加进去，让大家都知道（这个文件不是拿来训练的，就是给人看的，放心加）

## 打标签
按照上述流程打标签
**数据集结构**
```
data
├─ test.jpg
└─ test.json
```
就这么简单无脑

## 把U盘交给我即可，我统一转格式，然后我和明辉训练